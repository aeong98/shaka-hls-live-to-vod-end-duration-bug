services:
  - type: web
    name: shaka-hls-live-to-vod-end-duration-bug
    runtime: node
    plan: free
    buildCommand: npm ci
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_VERSION
        value: 22.19.0
